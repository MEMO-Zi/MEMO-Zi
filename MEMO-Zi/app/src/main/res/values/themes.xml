<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MEMOZi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor" tools:targetApi="l">#DFADB5</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="colorPrimary">@color/sub_purple</item>
    </style>

    <!-- Button -->
    <style name="Widget.Material3.Button" parent="">
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingHorizontal">22dp</item>
        <item name="android:background">@drawable/shape_7dp</item>
    </style>
</resources>